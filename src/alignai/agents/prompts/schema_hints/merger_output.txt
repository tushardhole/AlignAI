Your JSON must have exactly one top-level key "content". Its value is the full resume as one plain-text string (newlines between sections). Do not output contact, summary, experience, skills, or education as nested JSON objects or arrays — nesting makes responses too long and invalid. Only: {"content": "...full resume text..."}.
